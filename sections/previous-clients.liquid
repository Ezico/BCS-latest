<style>
  @media (max-width: 950px) {
    .hover-card-details h2 {
      font-size: 30px !important;
    }
  }
  .col {
    width: 48%;
    margin-top: 20px;
  }
  .img-hover-zoom img {
    width: 100%;
    transition: transform 0.6s ease;
  }
  .img-hover-zoom {
    height: 500px;
    overflow: hidden;
  }
  .hover-container {
    padding: 0;
    overflow: hidden;
  }
  .gap-40 {
    display: flex;
    gap: 40px;
  }
  .hover-container {
    padding: 0;
    position: relative;
    overflow: hidden;
  }
  .hover-card-details {
    position: absolute;
    bottom: 150px;
    z-index: 999;
  }
  .hover-container:hover {
    box-shadow: none;
  }
  .hover-card-details {
    text-align: center;
    padding-top: 10px;
    position: relative;
    bottom: 108px;
    z-index: 999;
  }
  .hover-card-details h2 {
    margin: 0;
    color: var(--Text---white, #fff);
    text-align: center;
    font-family: Cormorant;
    font-size: 40px;
    font-style: normal;
    font-weight: 700;
    line-height: normal;
    text-transform: uppercase;
  }
</style>
<div class="page-width">
  <h2 class="title" style="text-transform:uppercase">
    See Previous Designs for Clients
  </h2>
  <p class="desc" style="text-align: left; line-height:2">
    As a luxury baby furniture company, we understand the joy and excitement
    that comes with preparing for a new arrival. <br>
    That’s why we’re proud to offer a range of benefits to our valued customers
    to make the process a little easier.
  </p>

  <div class="row gap-40">
    {% for block in section.blocks %}
      {% assign images = block.settings.image | img_url: 'master' %}
      <div class="col">
        {% render 'services-card' title:{{block.settings.title}}, desc:{{block.settings.desc}}, image:{{images}}, %}
      </div>
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "services-items",
  "tag": "section",
  "class": "services",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [],
  "blocks": [
    {
      "type": "heading",
      "name": "Clients",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "background image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "title"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Services Item"
    }
  ]
}
{% endschema %}
